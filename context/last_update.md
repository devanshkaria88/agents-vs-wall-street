<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-08-16T13:02:16Z

The build is fanned out and running — here's the state of play:

**In flight (dynamic workflow, 3 agents):**
1. **UI overhaul** — full-bleed React Flow canvas as the page background (reactflow.dev style), dotted grid, dark node cards with header strips, dashed edges that ignite amber and glow when their target stage is running, floating minimal panels. The purple/teal gradient blobs get killed — that's the "AI-generated look" culprit.
2. **Backend** — `agent/config.json` (model, effort, runs, per-company skills, tool toggles including server-side `web_search` with a keep-off-for-the-offline-final-run note), `loop.py` reads it all; **`agent/skillgen.py`** — the dynamic skill-writer agent that studies a company's corpus and writes `agent/skills/<company>.md` itself (with backup + provenance header, preserving the driver-key contract); plus `/api/run` (spawn extraction/forecast/skillgen from the UI) and `/api/agent-config` (GET/PUT).
3. **Integrator** (after both) — Run buttons in the header, a Configure section on Reader nodes (skill checklist, tool toggles, system-prompt textarea, runs count), skill-writer status view, typecheck + smoke-test.

**Also done just now:** [docs/FINAL-RUN.md](docs/FINAL-RUN.md) — the mechanical 17:00→18:00 runbook, including the "everything is on fire" fallback (the committed workbooks are always uploadable).

I'll verify visually, fix whatever the agents got wrong, and screenshot for you the moment the workflow completes. Clock check: 14:05, judge window opens ~16:00 — comfortable.
