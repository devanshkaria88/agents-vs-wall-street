<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-08-16T14:37:54Z

The route file shows the UI agent mid-flight on the backtest wiring — I'll stay out of its files and let the verifier phase close the gaps. Status while the workflow finishes:

**Since your last message, all three asks are moving:**
1. **Event dialogs** — live and committed: clicking any feed tile opens a centered frosted dialog with the *full* content re-read from the frozen corpus (doc slices, search reruns, skill files) rendered as markdown, not the 400-char trace head. Path-gated to the corpus and skills directories.
2. **Feed reset per full run** — a live full run now filters the feed to events stamped after its start, so it visibly empties and refills.
3. **Backtests** — a 7-agent workflow is building it: the mechanism is a per-thread *date cutoff* inside the corpus tools themselves (docs dated on/after the held-out quarter's report date "don't exist" for that reader — no corpus copies, citations stay canonical, and backtest readers get **no web_search**, since the web knows the answers). Four miners are extracting verified holdout specs (guidance quote + actual quote + prior actual, all byte-exact) per company; the harness scores `|forecast−actual|` against a naive prior-quarter benchmark as the stand-in for the hidden Wall-Street denominator.

Also refreshed [architecture/index.html](architecture/index.html) (locks at 17:15) with what now exists: the adversarial-validator stage and its real first-flight story — it **blocked a run** that had regressed to the £9.0m finance-charge assumption, citing the H1 report byte-exact; we pinned the extracted £6.7m and its rerun confirmed 3/3 — plus the fullrun control plane, event observability, and the web-search triage policy. I'll add the backtest section with *real* accuracy numbers once the harness lands and I've run a couple of holdouts live.
