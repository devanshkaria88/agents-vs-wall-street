<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-08-16T16:25:13Z

Two things — one of them urgent: **your repo URL has a typo**. The field shows `agents-vs-wall-streetw` with a stray `w` at the end. It must be `https://github.com/devanshkaria88/agents-vs-wall-street`, exactly matching entry.json, or reviewers hit a 404.

On the harness question: the field is asking what agent scaffolding *produced this forecast*. Ours is genuinely custom — the runtime is our own Python tool loop built directly on the Anthropic SDK's `tool_runner` (that's `agent/loop.py`: search_corpus, read_doc, submit_drivers, with the JSONL tracing), feeding a deterministic Python engine. We did **not** use the Claude Agent SDK at runtime — that's a different thing (the SDK that powers Claude Code-style agents), and "Claude Code" was only our *build* harness, not what runs the forecast.

So in that dropdown, in order of preference:
1. **"Custom"** or "Own harness" if the list has it — that's the accurate answer
2. **"Anthropic SDK"** / "Anthropic API" if offered — our loop is built directly on it
3. If the list only offers big-name agent frameworks and none of the above, "Claude Agent SDK" is the least-wrong neighbor, but pick it only as a last resort — it slightly misdescribes us, and our architecture page proudly says we hand-rolled the loop precisely so the tool traces would be ours

Consistency check while you're in the form: Model = Claude Opus 5 is right (that's the runtime Reader/validator model; Fable 5 was the build-time model in Claude Code, recorded in entry.json). And fix that URL before submitting.
