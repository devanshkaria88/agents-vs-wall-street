<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-08-16T14:47:42Z

Everything you asked for this turn is live, verified, and committed — and the submission checker is green with all 12 cells filled. The full picture:

**Backtests (the new headline).** Every corpus tool now honors a per-thread date cutoff, so a backtest reader is genuinely time-traveled: documents from the held-out quarter's report date onward "do not exist for you" — reads refused, search hits suppressed, refusals logged in the trace — with no corpus copies and no web_search (the web knows the answers). Four ran live today, one per company, each launched-able from the new quarterly buttons panel:

| Holdout | Forecast | Actual | Miss | vs naive |
|---|---|---|---|---|
| Hays FY25 op profit | 45.0 | 45.6 | 1.3% | **×0.010** |
| Deere FY2025 net income | 5,000 | 5,027 | 0.5% | **×0.013** |
| HD FY26Q1 net sales | 41,251 | 41,765 | 1.2% | **×0.269** |
| ADI FY26Q2 revenue | 3,500 | 3,623 | 3.4% | **×0.266** |

The Hays one is the demo gem: street consensus compiled £56.4m against an actual of 45.6 — the system's guidance anchor missed by 0.6 where the street missed by 10.8. And the first HD run failed *usefully*: HD guides growth-%, not dollars, and the harness anchored 3.5 against 41,765 — I added an explicit `anchor_mode` so code applies growth guidance to the year-ago base (arithmetic stays out of the LLM). Twelve byte-verified holdouts ship in [holdouts.json](research/backtests/holdouts.json); the other eight are one click each in the panel.

**Your earlier asks**, all in: event tiles open as centered dialogs with the full markdown re-read from the frozen corpus (a portal escape was needed — the frosted panels' backdrop-filter traps `fixed` children); a live full run resets the feed to its own events; and the architecture page (locks **17:15**) now tells the true story — the validator-agent stage with its first-flight blocker, the fullrun control plane, web-search triage, and the backtest table with honest caveats (these score the raw anchor without beat-bias; ADI's 3.4% miss is precisely the beat the calibration layer adds).

**Runway:** submission is green now, so the remaining path is calm — final `npm run forecast` in the 17:15 window if you want a fresh log, then manual upload + entry form at 17:30–18:00. If you get a minute before judging, run a couple more backtest chips on ADI (its quarterly guidance history is the cleanest) so the panel shows a fuller scoreboard.
