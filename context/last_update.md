<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-08-16T14:10:22Z

Web search is now standing kit for the readers, with the dynamic triggering you asked for handled at the prompt-policy level:

**What changed:**
- **`agent/loop.py`** — a "web search triage" policy is appended to the reader's system prompt whenever the tool is armed: corpus-first always (corpus citations get byte-verified, web ones can't be); escalate to `web_search` *only* when a driver genuinely can't be resolved from the corpus or when cross-checking something that may have moved after the 2026-08-14 freeze. Web-sourced drivers must cite the URL + exact quoted sentence + date, and if the web contradicts corpus guidance, the reader keeps the corpus value and files the contradiction as evidence — the merge divergence gate stays the arbiter, so a web result can never silently override your reviewed anchors. The default config now has web_search on (your config already did).
- **ConfigPanel** — the scary "keep OFF for the offline final run" note is gone; I confirmed against the rules that public web info is explicitly allowed (RULES.md:22), and the final command (`npm run forecast`) never invokes readers anyway, so web search poses zero risk to the final-run window. The new copy states the corpus-first policy instead.
- The policy arming logs itself as a `load_skill` event, so the event feed shows exactly which runs had web reach — same traceability story as everything else.

Also verified the validator agent being built right now won't inherit the policy text (it shares the skill loader but not the tool, so it never gets told to search the web).

The big workflow is still running in the background — implementers have landed their files (you may have seen the graph restructure and glass changes flash into the working tree) and it's moving through integration verification and the three adversarial reviews. When it completes I'll apply findings, boot the dashboard for visual checks, and run a full end-to-end Full Run — which will now exercise the web-search triage live.
