<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-08-16T13:26:41Z

**Fixed and shipped.** Here's the full answer to your question:

**Was it actually re-reading?** No — and it shouldn't. "Run full forecast" is the deterministic half: engine → validator → workbook writer → official checker, consuming the already-extracted drivers. It takes ~3 seconds and produces identical numbers by design (that's the determinism story). Document *re-reading* is "Run extraction" — which, as it turns out, you'd also just clicked, and those Reader agents **are running right now** with your config (2 runs, web_search on, and they're the first readers to use the agent's self-written Hays skill).

**Why the UI showed nothing:** the 3-second forecast finished entirely between two polls, and its stages were already green — so there was literally no frame where anything changed. Now fixed, two ways:

1. **Freshness flash** — any stage whose artifact just changed flips to the amber running state for ~7 seconds, so a rerun visibly sweeps through calibration → calculators → validator → writer → checker with the glowing edges following it. Verified live: I fired a forecast and caught all five stages mid-sweep.
2. **Last-run chip** — Run Controls now permanently shows "● last forecast 14:24:49 · **RUN CLEAR**" (green dot, or red + FAILED), so even a blink-and-miss run leaves a receipt.

Click "Run full forecast" again and you'll see the sweep. I'm watching your Hays extraction in the background and will review its merge report when it completes — with web_search enabled, this run may pull in something the corpus doesn't have, and the divergence policy will flag anything that contradicts the reviewed anchors.
