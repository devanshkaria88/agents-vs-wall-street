<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-08-16T11:47:21Z

Two things worth noticing in the shape of it:

- **There's exactly one purple box.** The AI appears once, early, and only to gather evidence — everything downstream of the firewall is deterministic and replayable. That asymmetry *is* the architecture pitch to the judges.
- **The two ↻ reject loops are where reliability lives.** A quote that can't be re-found in its source file bounces back to the Reader with the reason; a value that fails units/range/cross-foot checks falls back toward the guidance anchor rather than shipping. Both kinds of rejection are logged, and judges explicitly score "rejected values" under Validation & reliability.

Every stage also drops its artifact on disk — `drivers.json`, the calibration report, per-metric traces, the run log — so the judge conversation can walk any of the 12 final numbers backwards to a quoted sentence in a named file.

That's the whole machine. Build it?
