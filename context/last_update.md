<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-08-16T16:52:05Z

Done and pushed (`80710aa`): **`npm run forecast:full`** now runs the entire pipeline — readers, firewall, vote & merge, calibration, calculators, adversarial validator, writer — for all four companies in one command. It's in the repo for judges and for you, tomorrow. The one-button per-company version is what the dashboard's Full run has been all day; this is the same thing as a single CLI command.

The **declared** entry stays exactly as recorded: `finalCommand: npm run forecast`, commit `e00f36e`, RUN CLEAR log in `logs/` — compliant, honest, already described accurately on the locked architecture page. If anyone asks "why doesn't the final command call the LLM," the answer is a feature, not an apology: *the agents ran all day and every number they produced is traced; the final run is deterministic replay of their reviewed output, so it cannot hallucinate, cannot flake, and reproduces bit-for-bit — and `forecast:full` exists when you want the whole organism live.*

Now forget the pipeline — two things matter before 18:00: all four workbooks showing as accepted on OpenStocks, and the private entry form submitted with entry.json + the architecture HTML. Confirm those two and we're done.
