<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-08-16T15:15:46Z

Pushed — all 15 commits are on GitHub (`1ab0945..2566709`), local and origin now in sync. The judges can see the real repo.

For the teammates, entry.json needs exactly three things per person:

1. **Full name**
2. **Email address** — must be valid and unique per member (the checker enforces this); it's used for event admin, results, and prize follow-up
3. **Their agreement** to that email use — the file's `emailUseConfirmed: true` asserts *everyone* listed consented, so ask them before I add them

Two structural notes: **you stay listed first** as primary contact (your name + email must match what you type into the submission form), and emails go *only* in entry.json — it's gitignored and stays out of the HTML and repo.

Paste me the two names + emails (and confirm they're okay with the email use) and I'll add them and re-run `check:entry`. Also worth knowing: the entry.json `teamMembers` list is about prize/entry administration, but the **social competition is per individual** — each teammate can enter their own X/LinkedIn post before 17:00, so you have three shots at those $500s, not one.
