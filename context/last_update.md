<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-08-16T16:28:58Z

Yes — the format is exact: our writer copies the official template and touches only `Summary!C7:C9`, and the file you tried to upload passes the organizers' *own* checker script on this machine. But I found the real fault line, and you now have two paths:

**First, check the boring explanation.** The genuine `HD-FY2026Q2.xlsx` is **6,061 bytes**. If the file in your browser came from GitHub's web page (the blob view saves an HTML page named `.xlsx`) or a stale download, it will be way off that size and will absolutely produce "No Summary tab found." Re-pick the file straight from the `submission/` folder (or the dashboard's Download button) and hit "Check it again."

**If the true file still fails, use `submission-fallback/`.** Here's why it might: the organizer templates were generated by a .NET OpenXML library, and openpyxl rewrites the entire internal XML in its own equally-valid-but-different dialect. Their web parser may only tolerate their own generator's style. So I built surgical copies: **byte-identical to the pristine templates** — same XML namespaces, same relationship IDs, everything — except the three `<v>` value insertions per Summary sheet. Same twelve numbers, verified by both openpyxl and the official checker:

```
HD 47,613 · 4.68 · 1  |  ADI 3,960 · 3.39 · 73.3  |  HAS 903.5 · 1.12 · 46  |  DE 12,543 · 4.84 · 556
```

Both sets are pushed (`9397a38`), so whichever machine is doing the upload can pull them. Try the original file first (correct provenance for the declared run), fall back to `submission-fallback/` the moment it argues. You have the time — go.
