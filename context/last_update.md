<!--
Cross-agent sync file. Auto-generated by hooks.
-->
agent: claude
timestamp: 2026-08-16T15:13:11Z

Here's the complete picture from the live page, cross-checked against our local state.

## What must be handed in (five things)

1. **entry.json** — private, *never committed*; uploaded via the "Submit team entry" form. Ours exists and **passes `check:entry`** ("The Truth", 1 member, hybrid build, models declared). One field is stale: `finalCommit: 612580f` — that's from this morning and must be updated to the real final commit after the final run.
2. **Repository + exact final commit** — judges read the repo tied to that hash. ⚠️ **We are 14 commits ahead of origin** — everything from this afternoon (fullrun, validator agent, backtests, HTML) is unpushed. This must be pushed before 18:00; say the word and I'll push.
3. **Architecture HTML** — self-contained ≤2MB, uploaded *with* entry.json on the same form (no hosting needed). Ours is current as of 15:55 (validator, backtest table, fullrun) and locks at **17:15**.
4. **Clear-run log** — timestamped, from the declared final commit, secrets-free. The final run in the 17:15 window produces this (`logs/run-*.log`, RUN CLEAR).
5. **Four workbooks** — already green in `submission/`, but the ones that count are regenerated by the final run, then **uploaded manually, one per matching Forecast Model**, starting 17:30. The dashboard's Download buttons (or the folder directly) give you the files.

## The sequence from here (now ~16:05)

**Now – 17:15** (judging conversations are running now):
- Give me the go-ahead to **push the 14 commits** — that alone fixes the biggest gap.
- Optional but free: the **social competition closes at 17:00** — $500 each for an X post (quote the launch post, tag @_openstocks @primerapp_ @AITinkerers @openai) and a LinkedIn repost (tag Primer, OpenAI, AI Tinkerers), link pasted into their form.

**17:15 – 17:30 (final-run window opens):** I commit the working tree (there's one modified `agent/config.json` from your UI toggles), note the hash, run `npm run forecast` once from it → RUN CLEAR log, set `finalCommit` to that hash in entry.json, re-run `npm run check:submission`, and push. If it crashes, retries before 18:00 are allowed (new commit → update entry.json).

**17:30 – 18:00 (all manual, all you — I'm not permitted to and won't touch the upload forms):**
- Upload the four workbooks to their matching Forecast Models on OpenStocks.
- Submit the private team entry form: agent name, your name, your email, repo URL (all four must match entry.json exactly), plus the entry.json file and `architecture/index.html`. Re-submitting before 18:00 replaces the record, so an early submission is a safe checkpoint — you could file it right after the final run and re-file only if something changes.

One rule worth repeating from the page: *"Forecast uploads are manual. Your agent must not submit to OpenStocks programmatically"* — so every browser step at 17:30+ is yours; I'll have the files, the log, and entry.json staged and verified so it's pure clicking.

Want me to push the commits now?
